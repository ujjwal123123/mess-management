extends layout

block content
  .level
    .level-left
      h1.title.is-1 Students
    .level-right
      a(href="/student/download") Download

  #students-table

  script(type="text/javascript", src="/js/delete.js")
  script.
    new Tabulator('#students-table', {
      data: !{ students }, // assign data to table
      autoColumns: true, // create columns from data field names
    });
